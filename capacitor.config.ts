import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'IonicDemoReactIssue',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
